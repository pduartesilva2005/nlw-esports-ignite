import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center'
  },

  logo: {
    width: 202,
    height: 100,
    marginTop: 62,
    marginBottom: 32
  },

  contentList: {
    paddingLeft: 20,
    paddingRight: 32
  }
});
